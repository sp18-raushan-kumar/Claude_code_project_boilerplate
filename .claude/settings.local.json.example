{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Personal overrides. Copy this file to settings.local.json (which is gitignored, so it is NOT shared with the team). Permission rules here MERGE with .claude/settings.json. Use it for your own preferences without affecting teammates.",
  "permissions": {
    "allow": [
      "Bash(git push *)"
    ]
  }
}
