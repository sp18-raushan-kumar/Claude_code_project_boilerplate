---
name: example-skill
description: <when Claude should AUTO-USE this skill — the tasks/phrases that should trigger it, e.g. "when adding a new API endpoint">
allowed-tools: "Read, Edit"
---

<!--
EXAMPLE skill. Copy this whole folder to .claude/skills/<your-skill>/, rename it, edit it,
and delete this example.

Skills are know-how Claude triggers ON ITS OWN when a task matches the `description` above
(no slash command needed). The folder name is the skill name. Use skills to encode
repeatable procedures so they're applied consistently. The body below is what Claude
follows when the skill activates.
-->

# <Skill name> — <what it does>

## Steps
1. <step one — name the files/dirs involved>
2. <step two>
3. <verify it worked>

## Gotchas
- <the thing people get wrong>
